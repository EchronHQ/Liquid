<?php
declare(strict_types=1);

/**
 * @var $block \Liquid\Framework\View\Element\Template
 * @var $this \Liquid\Framework\View\TemplateEngine
 */

$viewModel = $block->getViewModel('debug', \Liquid\Admin\ViewModel\DebugViewModel::class);

$entities = $viewModel->x();
foreach ($entities as $entity) {
    echo '<div>' . $entity->getUrlPath() . '</div>';
}
