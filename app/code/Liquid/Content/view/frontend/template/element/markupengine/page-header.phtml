<?php
declare(strict_types=1);

/** @var $block Template */

use Liquid\Content\Block\Element\MarkupEngine\PageSectionTag;
use Liquid\Framework\View\Element\Template;


$properties = PageSectionTag::PROPERTIES;

$pageSectionTags = [];
foreach ($properties as $property) {
    if ($block->hasData($property)) {
        $value = $block->getData($property);
        if (!empty($value)) {
            $pageSectionTags[] = $property . '="' . $value . '"';
        }
    }
}
?>
<header>
    <page-section <?= implode(' ', $pageSectionTags) ?>>
        <?php
        $content = $block->getData('content');
        if ($content !== null) {
            // TODO: should we log that the content is empty?
            echo $content;
        }
        ?>
    </page-section>
</header>
